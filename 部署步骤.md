# 部署步骤：把 V13 发布到 GitHub Pages 并安装到手机

> 本文件夹（unstable-2048-v13）已经是完整的 PWA，无需再改代码。
> 所有资源都是 `./` 相对路径，部署在 `/unstable-2048/` 子目录也能正常工作。

---

## 一、发布到 GitHub Pages（两种方式二选一）

### 方式 A：网页上传（不需要命令行，推荐第一次用）

1. 登录 [github.com](https://github.com)，右上角 `+` → `New repository`。
2. 仓库名填 `unstable-2048`，选择 `Public`，点击 `Create repository`。
3. 在新仓库页面点击 `uploading an existing file`（或 `Add file → Upload files`）。
4. **把本文件夹里的全部内容拖进去**（是文件夹里的内容，不是文件夹本身！）：
   - 必须保证 `index.html` 位于仓库第一层，而不是 `unstable-2048-v13/index.html` 这样多套一层
   - 需要上传：`index.html`、`manifest.webmanifest`、`sw.js`、`.nojekyll`、`css/`、`js/`、`vendor/`、`icons/`
   - `版本说明.txt`、`部署步骤.md` 可传可不传，不影响运行
5. 点 `Commit changes` 等上传完成。
6. 进入仓库 `Settings` → 左侧 `Pages`：
   - `Source: Deploy from a branch`
   - `Branch: main`，`Folder: / (root)`
   - 点 `Save`
7. 等约 1 分钟，刷新该页面会显示发布地址：

```text
https://你的GitHub用户名.github.io/unstable-2048/
```

### 方式 B：Git 命令行

在本文件夹打开 Git Bash，依次执行（把"你的用户名"换成你的）：

```bash
git init
git add .
git commit -m "Initial PWA release: unstable 2048 v13"
git branch -M main
git remote add origin https://github.com/你的用户名/unstable-2048.git
git push -u origin main
```

然后同方式 A 的第 6 步开启 Pages。

---

## 二、发布后先在电脑上验证

浏览器打开 `https://你的用户名.github.io/unstable-2048/`：

1. 游戏能正常加载、能玩。
2. 按 F12 打开开发者工具 → `Application` 标签：
   - `Manifest`：能看到应用名"不稳定的2048"和图标
   - `Service Workers`：状态为 `activated`（可能需要刷新一次）
   - `Cache Storage`：`unstable-2048-v1` 内有全部 js/css/图标
3. F12 → `Network` 标签勾选 `Offline`（离线模拟）后刷新页面，游戏仍能加载 = 离线缓存成功。

---

## 三、安装到手机

**Android（Chrome）：**
1. 用 Chrome 打开发布地址，等游戏完整加载一次（最好玩几秒）。
2. Chrome 菜单（右上角⋮）→ `安装应用` 或 `添加到主屏幕`。
3. 桌面出现"不稳定2048"图标，点开是全屏游戏。

**iPhone（Safari）：**
1. 用 Safari 打开发布地址，等加载完成。
2. 底部分享按钮（方框+箭头）→ `添加到主屏幕` → 确认。
3. 主屏幕图标点开即全屏运行。

**离线验证：** 开启飞行模式，从桌面图标重新打开游戏，能正常玩 = 成功。

---

## 四、以后更新游戏怎么办

1. 修改代码后，**必须**把 `sw.js` 第 5 行的缓存版本号递增：
   ```js
   const CACHE_NAME = 'unstable-2048-v1';  →  'unstable-2048-v2';
   ```
   （新增了文件的话，还要把新文件加进 `sw.js` 的 `APP_SHELL` 清单）
2. 重新上传/`git push` 改动的文件。
3. 手机上打开一次游戏（联网状态），新版本会自动替换旧缓存；如果没变化，关掉游戏重开一次即可。

---

## 五、常见问题

| 现象 | 原因与解决 |
|---|---|
| 手机上添加主屏幕后打开是空白 | 第一次访问时没等资源加载完就离线了；联网重新打开一次，确认 Cache Storage 有内容 |
| Service Workers 显示注册失败 | 必须通过 `https://` 访问（GitHub Pages 自带）；不能用 `file://` 直接打开 |
| 图标显示为网页截图而不是金色方块 | manifest 未生效；确认 `manifest.webmanifest` 和 `icons/` 目录都在仓库第一层 |
| 更新后手机还是旧版本 | `CACHE_NAME` 没递增，或没联网重新打开游戏 |
| 最高分/倍速设置丢了 | 清除了浏览器数据；PWA 的 localStorage 在正常使用中会保留 |
